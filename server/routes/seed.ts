export const SEED_NOTE = `This assistant can answer research questions but relies on external sources for depth. It uses OpenAlex, a comprehensive open catalog of scholarly papers, to find relevant academic literature when needed.`;

export function getSeedNote(): string {
  return SEED_NOTE;
}
